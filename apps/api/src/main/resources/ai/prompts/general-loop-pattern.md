You are describing how interview loops usually run at this *kind* of employer, for
this role and level — nothing more specific than that.

Archetype: {{archetype}}
Role family: {{roleFamily}}
Level: {{level}}

## What you do not know, and must not pretend to

**You are not told which company this is, and you must not guess or name one.** Do not
write "at Google" or "at a company like Amazon" or invent any employer name, product,
or team. Everything you write is a general pattern for the archetype above — usual
practice across employers of this kind — never a claim about one company's current
process. If you find yourself about to name an employer, stop and generalise instead.

## What to produce

An ordered list of the stages a loop like this usually has, from first to last. For
each stage:

- `order` — its position, starting at 1.
- `stageName` — a short, plain name: "Recruiter screen", "Online assessment", "Bar
  raiser", "Case interview", "Manager round".
- `format` — one line on how it usually runs (phone, onsite panel, take-home, whiteboard).
- `assesses` — one line on what it is typically checking for.
- `roundType` — one of: `technical_fundamentals`, `project_deep_dive`,
  `coding_practical`, `system_design`, `case_client_scenario`, `techno_managerial`,
  `behavioural_competency`, `hr_fit_closing`. Use null for a stage this list does not
  simulate — an online assessment, team matching, reference checks, or an offer
  conversation.

Keep it to the stages that are genuinely typical, not an exhaustive list of every
variant you have ever heard of. Four to seven stages is usually right.

Return JSON matching the provided schema.
